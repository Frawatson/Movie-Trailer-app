import React from 'react'

/**
 * Renders a message indicating that the page could not be found.
 *
 * @return {JSX.Element} A div element containing the message.
 */
const NotFound = () => {
  return (
    <div>
        This page could not be found
    </div>
  )
}

export default NotFound